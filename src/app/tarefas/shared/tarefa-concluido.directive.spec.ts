import { TarefaConcluidoDirective } from './tarefa-concluido.directive';

describe('TarefaConcluidoDirective', () => {
  it('should create an instance', () => {
    const directive = new TarefaConcluidoDirective(null!);
    expect(directive).toBeTruthy();
  });
});
